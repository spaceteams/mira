{
  "name": "lyra-sql",
  "version": "0.0.1",
  "description": "generate typesafe typescript apis from sql statements",
  "workspaces": [
    "app",
    "clients/*",
    "packages/*"
  ],
  "scripts": {
    "format": "deno fmt",
    "lint": "deno fmt --check && deno lint",
    "test": "vitest",
    "test:ci": "vitest run",
    "check": "tsc --noEmit",
    "publish": "npm publish --dry-run --workspace app && npm publish --dry-run --workspace clients",
    "build": "npm run build --workspace app && npm run build --workspace clients"
  },
  "devDependencies": {
    "esbuild": "0.18.15",
    "esbuild-node-tsc": "2.0.5",
    "typescript": "5.1.6",
    "vite": "4.4.4",
    "vitest": "0.33.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zod": "^3.21.4"
  }
}