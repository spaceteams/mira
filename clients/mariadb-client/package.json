{
  "name": "mira-mariadb-client",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/clients/mariadb-client/index.d.ts",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "clean": "rm -rf build dist",
    "build": "rollup -c ../../rollup.config.mjs"
  },
  "dependencies": {
    "mariadb": "3.2.0",
    "mira-core": "0.0.1"
  },
  "devDependencies": {
    "testcontainers": "9.12.0"
  }
}
