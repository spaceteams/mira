{
  "name": "@lyra/mariadb-client",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/clients/mariadb-client/index.d.ts",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "clean": "rm -rf build dist",
    "bundle": "rollup -c ../../rollup.config.mjs",
    "build": "npm run clean && npm run bundle"
  },
  "dependencies": {
    "mariadb": "3.2.0",
    "parser": "file:../../packages/model"
  },
  "devDependencies": {
    "testcontainers": "9.12.0"
  }
}