{
  "name": "@lyra/postgresql-client",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/clients/postgresql-client/index.d.ts",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "clean": "rm -rf build dist",
    "bundle": "rollup -c ../../rollup.config.mjs",
    "build": "npm run clean && npm run bundle"
  },
  "dependencies": {
    "parser": "file:../../packages/model",
    "pg": "8.11.1"
  },
  "devDependencies": {
    "@types/pg": "8.10.2",
    "testcontainers": "9.12.0"
  }
}