{
  "name": "app",
  "version": "0.0.1",
  "main": "build/index.js",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest run",
    "build": "esbuild src/index.ts --outdir=build --bundle --minify --platform=node"
  },
  "dependencies": {
    "chokidar": "3.5.3",
    "glob": "10.3.3",
    "parser": "file:../packages/parser"
  },
  "devDependencies": {
    "@types/yargs": "17.0.24"
  }
}